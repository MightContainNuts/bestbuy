[pytest]
# Optional: specify test paths if you want to limit the tests to specific directories
testpaths = tests

# Optional: specify any pytest arguments you want to include, like verbosity or specific options
addopts = -v
